---
description: Run all code quality checks before committing. Detects console.log statements, validates commit message format, checks current branch, and scans for hardcoded secrets.
---

Run the `code-guardian` agent to perform all 4 code quality checks on this project.
